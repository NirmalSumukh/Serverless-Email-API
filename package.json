{
  "name": "email-api",
  "version": "1.0.0",
  "description": "Serverless email API with offline execution",
  "main": "src/handlers/emailHandler.js",
  "scripts": {
    "start": "serverless offline start",
    "deploy": "serverless deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemailer": "^6.9.15",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "serverless": "^4.0.0",
    "serverless-offline": "^14.0.0"
  }
}
